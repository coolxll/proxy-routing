interface:
  display_name: "Proxy Routing 运维"
  short_description: "维护代理规则、Sublink 模板并安全落地 ShellCrash"
  default_prompt: "Use $proxy-routing-ops to update proxy rules and safely deploy them through SublinkPro to ShellCrash."
